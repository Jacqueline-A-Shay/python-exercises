np.array()
 x = np.array()
 y = np.array() 
 np.full(10,3.3) # get 10個3.3們

 mask_above_ten = numbers >= 10
 numbers[mask_above_ten] >> return all the >= 10 from the numbers array

 np.median(numbers)
 np.random.randint()
 numbers.mean()
 numbers.min()
 numbers.max()
 
x.# drop down list having all the potential methods
# measure magnitude of two different vectors # dot product

#timeit followed by some operations, time the operations